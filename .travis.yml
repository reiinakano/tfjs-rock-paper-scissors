language: node_js
node_js:
  - "7"
install:
  - "yarn prep"
script:
  - "yarn build"
  - "ls dist"
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
